// General
smoothness = 50;        // Circle resolution
hole_tolerance = 0.5;    // 0.5mm tolerance for holes

manufacturer_tolerance = 0.4;

// PCB dimensions
pcb_size = 50;           // 5cm square PCB
extrude_height = 4;     // 4mm extrusion height


display_module_width = 10;        // Width (shorter side)

mainboard_thickness = 4;
lcd_holder_width = display_module_width;

